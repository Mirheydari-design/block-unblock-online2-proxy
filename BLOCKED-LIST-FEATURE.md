# 🚫 راهنمای قابلیت "لیست بلاک شده‌ها"

## 📋 خلاصه

پنل ادمین حالا یک بخش جدید و قدرتمند دارد: **لیست بلاک شده‌ها**

این بخش به شما امکان می‌دهد:
- ✅ لیست تمام کاربران بلاک شده را ببینید
- ✅ لیست تمام پست‌های بلاک شده را ببینید
- ✅ مستقیم از لیست، هر کاربر یا پست را Unblock کنید
- ✅ جستجو در لیست‌ها
- ✅ کپی کردن ID با یک کلیک
- ✅ مشاهده تاریخ، زمان و دلیل بلاک

---

## 🎨 ویژگی‌های بصری

### تب‌های زیبا
```
┌─────────────────────────────────────────┐
│ 👤 کاربران بلاک شده (5)  │  📝 پست‌های بلاک شده (12)
└─────────────────────────────────────────┘
```

### کارت‌های اطلاعاتی
هر آیتم در لیست یک کارت زیبا با اطلاعات کامل دارد:

```
┌─────────────────────────────────────────────┐
│ 👤 12345                                     │
│ 📝 از طریق پست: d58d983a-...               │
│ 🕒 ۱۴۰۴/۰۸/۳۰ - ۱۶:۴۵                       │
│ 💬 محتوای نامناسب                           │
│                            [📋] [✅ Unblock] │
└─────────────────────────────────────────────┘
```

---

## 🔧 نحوه استفاده

### ۱. مشاهده لیست کاربران بلاک شده

1. به پنل ادمین برو
2. به بخش **"🚫 لیست بلاک شده‌ها"** scroll کن
3. تب **"👤 کاربران بلاک شده"** به صورت پیش‌فرض فعال است
4. تمام کاربران بلاک شده را می‌بینی

### ۲. مشاهده لیست پست‌های بلاک شده

1. روی تب **"📝 پست‌های بلاک شده"** کلیک کن
2. تمام پست‌های بلاک شده را می‌بینی

### ۳. Unblock کردن مستقیم

روی دکمه **"✅ Unblock"** کنار هر آیتم کلیک کن:
- یک تاییدیه نشون داده می‌شه
- درخواست به سرور ارسال می‌شه
- در صورت موفقیت، آیتم از لیست حذف می‌شه

### ۴. کپی کردن ID

روی دکمه **"📋"** کلیک کن:
- ID به clipboard کپی می‌شه
- یک پیام موفقیت نشون داده می‌شه

### ۵. جستجو در لیست

در باکس جستجو تایپ کن:
- جستجو در ID
- جستجو در دلیل (reason)
- فیلتر real-time

### ۶. بروزرسانی لیست

روی دکمه **"🔄 بروزرسانی"** کلیک کن تا لیست refresh شود.

---

## 💾 ذخیره‌سازی داده‌ها

### localStorage (فعلی)

**مزایا:**
- ✅ کار می‌کند بدون نیاز به API اضافی
- ✅ سریع و instant
- ✅ بدون نیاز به اینترنت برای مشاهده لیست

**محدودیت‌ها:**
- ⚠️ فقط در همین مرورگر قابل دسترسی است
- ⚠️ اگر cache مرورگر پاک شود، لیست پاک می‌شود
- ⚠️ قابل همگام‌سازی بین دستگاه‌ها نیست

### راه‌حل برای Global Access

اگر می‌خواهید لیست را در همه جا ببینید، باید API endpoints زیر را اضافه کنید:

```javascript
// دریافت لیست کاربران بلاک شده
GET /api/admin/block/users
Response: [
  {
    userId: "12345",
    postId: "...",
    reason: "...",
    blockedAt: "2024-..."
  }
]

// دریافت لیست پست‌های بلاک شده
GET /api/admin/block/posts
Response: [
  {
    postId: "d58d983a-...",
    reason: "...",
    blockedAt: "2024-..."
  }
]
```

---

## 🔄 Sync با Block/Unblock Actions

### خودکار سازی

لیست **خودکار** با اکشن‌های Block/Unblock همگام می‌شود:

#### وقتی یک کاربر را Block می‌کنید:
```javascript
// شما روی "Block User" کلیک می‌کنید
↓
// درخواست به API ارسال می‌شود
↓
// در صورت موفقیت
↓
// کاربر به "لیست بلاک شده‌ها" اضافه می‌شود ✅
```

#### وقتی یک کاربر را Unblock می‌کنید:
```javascript
// شما روی "Unblock User" کلیک می‌کنید
↓
// درخواست به API ارسال می‌شود
↓
// در صورت موفقیت
↓
// کاربر از "لیست بلاک شده‌ها" حذف می‌شود ✅
```

همینطور برای پست‌ها!

---

## 🎯 کاربردها

### سناریو ۱: بررسی دوباره
```
"چند روز پیش یک کاربر رو بلاک کردم، ولی حالا نمی‌دونم کی بود!"
↓
به لیست بلاک شده‌ها برو و پیداش کن 🔍
```

### سناریو ۲: Unblock سریع
```
"می‌خوام چند نفر رو که اشتباهی بلاک کردم، آنبلاک کنم"
↓
از لیست بلاک شده‌ها یکی یکی آنبلاک کن ⚡
```

### سناریو ۳: مدیریت دسته‌ای
```
"می‌خوام ببینم چه کسایی بلاک هستن و چرا"
↓
لیست کامل با دلیل و تاریخ در اختیار داری 📊
```

### سناریو ۴: گزارش‌گیری
```
"چند نفر این ماه بلاک شدن؟"
↓
به تاریخ‌ها در لیست نگاه کن 📅
```

---

## 🎨 طراحی UI/UX

### اصول طراحی استفاده شده:

1. **کارت‌های اطلاعاتی** - اطلاعات در یک نگاه قابل مشاهده
2. **Hover Effects** - تجربه تعاملی بهتر
3. **Animation** - ورود نرم آیتم‌ها
4. **Color Coding** - رنگ‌های متمایز برای user/post
5. **Responsive** - در تمام سایزها کار می‌کند
6. **Search Real-time** - جستجوی فوری بدون تاخیر

### رنگ‌بندی:

- 🟣 **بنفش** - تب فعال و دکمه‌های اصلی
- 🔵 **آبی** - دکمه Unblock
- ⚫ **خاکستری** - دکمه کپی
- 🟡 **زرد** - نمایش دلیل (reason)

---

## 📱 Responsive Design

لیست در تمام سایزها کاملاً responsive است:

- 💻 **Desktop** - نمایش کامل با فاصله مناسب
- 📱 **Mobile** - کارت‌ها به صورت عمودی stack می‌شوند
- 🖥️ **Tablet** - بهینه‌سازی شده برای تبلت

---

## 🔒 امنیت

- ✅ هر Unblock نیاز به تایید کاربر دارد
- ✅ تمام درخواست‌ها از طریق Worker Proxy ارسال می‌شوند
- ✅ X-Admin-Token همچنان لازم است
- ✅ لاگ کامل از هر عملیات ثبت می‌شود

---

## 🚀 آینده: API Backend

اگر می‌خواهید این قابلیت را به API Backend وصل کنید:

### گام ۱: اضافه کردن Endpoints

در backend خود دو endpoint اضافه کنید:

```javascript
// دریافت لیست کاربران بلاک شده
app.get('/api/admin/block/users', authenticateAdmin, async (req, res) => {
  const users = await db.query(`
    SELECT user_id as userId, reason, blocked_at as blockedAt
    FROM user_blocks
    ORDER BY blocked_at DESC
  `);
  res.json(users);
});

// دریافت لیست پست‌های بلاک شده
app.get('/api/admin/block/posts', authenticateAdmin, async (req, res) => {
  const posts = await db.query(`
    SELECT post_id as postId, reason, blocked_at as blockedAt
    FROM post_blocks
    ORDER BY blocked_at DESC
  `);
  res.json(posts);
});
```

### گام ۲: آپدیت کد JavaScript

در `admin.html`، توابع `getBlockedUsers` و `getBlockedPosts` را تغییر دهید:

```javascript
async function getBlockedUsers() {
  try {
    const response = await fetch(`${API_BASE_URL}/users`, {
      headers: { 'X-Admin-Token': ADMIN_TOKEN }
    });
    return await response.json();
  } catch (e) {
    return [];
  }
}

async function getBlockedPosts() {
  try {
    const response = await fetch(`${API_BASE_URL}/posts`, {
      headers: { 'X-Admin-Token': ADMIN_TOKEN }
    });
    return await response.json();
  } catch (e) {
    return [];
  }
}
```

---

## 📊 آمار و اطلاعات

در بالای هر تب، تعداد کل آیتم‌ها نمایش داده می‌شود:

```
👤 کاربران بلاک شده (23)
📝 پست‌های بلاک شده (47)
```

---

## 💡 نکات مهم

1. **لیست فوری آپدیت می‌شود** - بعد از هر block/unblock
2. **جستجو case-insensitive است** - حروف بزرگ و کوچک فرقی ندارد
3. **ID های UUID خودکار تشخیص داده می‌شوند**
4. **دکمه کپی از navigator.clipboard استفاده می‌کند**
5. **تاریخ و زمان به صورت شمسی نمایش داده می‌شود**

---

## 🎉 تمام!

حالا شما یک **پنل مدیریت کامل** با قابلیت‌های زیر دارید:

- ✅ Block/Unblock کاربران
- ✅ Block/Unblock پست‌ها
- ✅ Block کاربر با Post ID
- ✅ **لیست کامل بلاک شده‌ها (جدید! 🔥)**
- ✅ Unblock مستقیم از لیست
- ✅ جستجو و فیلتر
- ✅ لاگ‌گیری کامل
- ✅ بدون مشکل CORS

**لذت ببرید! 🚀**

