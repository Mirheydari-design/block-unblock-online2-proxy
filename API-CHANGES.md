# 🆕 تغییرات جدید API - بلاک کاربر با Post ID

## خلاصه تغییرات

اندپوینت `/api/admin/block/user` حالا یک قابلیت قدرتمند جدید دارد:

**می‌توانید بدون دانستن User ID، تنها با داشتن Post ID، صاحب آن پست را بلاک کنید!**

---

## 📌 قبل از تغییر

```json
POST /api/admin/block/user
{
  "userId": "12345",
  "action": "block",
  "reason": "محتوای نامناسب"
}
```

**محدودیت:** باید User ID را می‌دانستید.

---

## 🎉 بعد از تغییر

### روش ۱: بلاک با User ID (مثل قبل)

```json
POST /api/admin/block/user
{
  "userId": "12345",
  "action": "block",
  "reason": "محتوای نامناسب"
}
```

### روش ۲: بلاک با Post ID (جدید! 🔥)

```json
POST /api/admin/block/user
{
  "postId": "d58d983a-cd29-419b-8329-7f60e9e78c6e",
  "action": "block",
  "reason": "پست نامناسب - صاحب پست بلاک می‌شود"
}
```

**سرور خودکار:**
1. صاحب پست را پیدا می‌کند
2. آن کاربر را بلاک می‌کند
3. تمام پست‌های او را مخفی می‌کند

### روش ۳: ارسال هر دو (اختیاری)

```json
POST /api/admin/block/user
{
  "userId": "12345",
  "postId": "d58d983a-cd29-419b-8329-7f60e9e78c6e",
  "action": "block",
  "reason": "بلاک با هر دو شناسه"
}
```

**نیاز:** حداقل یکی از `userId` یا `postId` باید ارسال شود.

---

## 💻 تغییرات در admin.html

پنل ادمین حالا دو فیلد در بخش "مدیریت کاربران" دارد:

### فیلد ۱: User ID
```
شناسه کاربر (User ID):
[                                    ]
```

### فیلد ۲: Post ID (جدید!)
```
یا شناسه پست (Post ID) (برای بلاک کردن صاحب پست):
[                                    ]
💡 اگر Post ID وارد کنید، سرور خودکار صاحب پست را پیدا کرده و بلاک می‌کند
```

---

## 🎯 کاربردها

### قبلاً:
1. کاربر پست نامناسبی می‌بیند
2. باید به پروفایل کاربر رفت
3. User ID را کپی کرد
4. در پنل ادمین وارد کرد
5. بلاک کرد

### حالا:
1. کاربر پست نامناسبی می‌بیند
2. فقط Post ID را کپی می‌کند
3. در پنل ادمین وارد می‌کند
4. بلاک می‌کند ✨

**۵ مرحله → ۴ مرحله | سریع‌تر و راحت‌تر!**

---

## 📊 مثال واقعی

فرض کنید یک پست با محتوای نامناسب دارید:

```
URL پست: https://mahdaviat.metafa.ir/post/d58d983a-cd29-419b-8329-7f60e9e78c6e
```

### در پنل ادمین:

**گزینه ۱:** کپی کردن UUID از URL
```
Post ID: d58d983a-cd29-419b-8329-7f60e9e78c6e
```

**گزینه ۲:** پیست کردن URL کامل (پنل خودکار UUID را استخراج می‌کند)
```
Post ID: https://mahdaviat.metafa.ir/post/d58d983a-cd29-419b-8329-7f60e9e78c6e
```

روی **🚫 Block User** کلیک کنید → صاحب پست بلاک می‌شود! ✅

---

## 🔒 امنیت

- همچنان نیاز به **X-Admin-Token** دارد
- تمام قوانین احراز هویت قبلی همچنان معتبر است
- لاگ‌ها شامل هر دو `userId` و `postId` می‌شوند

---

## 📝 تفاوت با /api/admin/block/post

| اندپوینت | هدف | نتیجه |
|---------|------|-------|
| `/api/admin/block/post` | بلاک کردن یک **پست خاص** | فقط آن پست مخفی می‌شود |
| `/api/admin/block/user` با `postId` | بلاک کردن **صاحب پست** | تمام پست‌های آن کاربر مخفی می‌شوند |

---

## ✅ چک‌لیست پیاده‌سازی

- [x] Worker Proxy آماده شد
- [x] admin.html آپدیت شد (پشتیبانی از postId)
- [x] لاگ‌گیری آپدیت شد
- [x] پیام‌های موفقیت/خطا آپدیت شدند
- [x] نمایش لاگ‌ها در UI آپدیت شد
- [x] مستندات آماده شد

---

**تاریخ به‌روزرسانی:** ۳۰ مرداد ۱۴۰۴  
**نسخه API:** v2.0 (با قابلیت بلاک از طریق Post ID)

